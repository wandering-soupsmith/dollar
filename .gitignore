# Environment variables (never commit real secrets: keys, Safe addresses, RPC URLs)
.env
.env.*

# Foundry
contracts/out/
contracts/cache/
contracts/broadcast/
# Coverage report (regenerated by `forge coverage`; consumed by ci/coverage_gate.py)
contracts/lcov.info

# Slither / crytic-compile artifacts (NOT slither.config.json — that stays tracked)
contracts/crytic-export/
contracts/slither.out.json
contracts/slither.err.txt

# Dependencies
node_modules/
contracts/lib/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Build outputs
dist/
build/

# Logs
*.log
npm-debug.log*
docs/

local

# Local editor/AI tooling config (personal, not committed)
.claude/
CLAUDE.md